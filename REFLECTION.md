# REFLECTION · ForgeLoop


---

本项目用 Superpowers 方法论约束的工作流，交付了一个自研 coding agent harness（ForgeLoop）。最深的体会是：当 LLM 能写出大部分样板代码时，工程师的价值并不在「多敲几行」，而在于定义什么叫正确、什么必须由确定性代码保证、以及如何证明系统在没有模型运气加持时仍然成立。

## Superpowers 技能：哪些有用，哪些形式大于实质

对我帮助最大的是 **brainstorming → writing-plans** 的强制顺序：在写循环之前先把「六个维度最低实现 + 一个重点深入」写成不变量。这直接避免了我一上来就接 LangChain Agent 的诱惑——那条路在课程判据下几乎必挂。`test-driven-development` 的纪律对 harness 尤其关键：护栏、分类器、主循环在 MockLLM 下必须红绿可复现，否则你根本分不清「机制写进了代码」还是「碰巧模型听劝」。

形式感更强的部分是「每个 task 一个新鲜 subagent + 独立 worktree/PR」。在单人期末、单机无 Docker 的约束下，严格并行 worktree 的边际收益低于成本。我选择在 `AGENT_LOG.md` 记录偏离理由，同时仍按 PLAN 的 task 粒度推进与验证。这让我意识到：Superpowers 假设你有足够的并行预算与评审带宽；课时压缩时，它的脚手架会变成清单表演，除非你主动裁剪并解释。

## TDD：阻碍还是放大器？

在 AI 协作下，TDD 首先是**放大器**，尤其针对治理与反馈：`guardrail(Action(...))` 与 `classify_failure_message(...)` 不需要模型参与，测试即规格。主循环的「失败回灌后下一步动作改变」也能用脚本化 MockLLM 证明——这正是 §A.4(C) 的精神。

它也会变成阻碍，当 task 颗粒度切到「先写一个必败测试」时，若 SPEC 对接口命名摇摆，会出现测试与实现一起改两轮的浪费。我的缓解办法是：先在 SPEC 冻结 `AgentAction` / `GuardDecision` / `FeedbackEvent` 三个核心类型，再允许 AI 填肉。没有这层冻结，TDD 会退化成「和模型一起改签名」。

## Subagent 自主时长与 task 颗粒度

同会话内按模块派发时，大约能稳定推进 1–2 个中等 task（例如护栏 + 反馈分类）而不明显跑题；一旦进入「WebUI + HITL + 凭据」交叉面，模型容易把 kernel 逻辑写进路由。最优颗粒度对我而言是：**一个可独立 pytest 文件证明的机制**，而不是「把 API 做完」。PLAN 里 Task 1–7 偏理想；Task 8 主循环是整合点，必须人工盯规格合规。

## SPEC / PLAN 质量如何影响实现：一个具体案例

早期草稿里，`run_shell` 与 `run_tests` 职责含糊。若按含糊版实现，subagent 会让模型用 shell 跑 pytest，然后在提示词里写「请根据输出自我修正」——这在评分上不算反馈机制。冷读 SPEC 后我改成：`run_tests` 必须走 `TestSensor` 并产出 `FeedbackEvent` 注入下一轮上下文。随后单测明确断言「第一步 run_tests 带 feedback，第二步动作变为 write_file，且第二次 LLM 调用的 prompt 含 Feedback」。这是「规约一句歧义 → 实现滑向提示词方案」的典型案例，也证明冷启动/自审的价值。

## 有效的 prompt / context 策略

最有效的约束不是长提示，而是：**把不可谈判的边界写成代码接口 + 测试名**。例如测试名 `test_loop_feedback_changes_action` 本身就是给实现 agent 的规格。其次，在上下文中反复粘贴课程原句「移除真实 LLM 后机制仍可单测」，比写一千字架构散文更能防止滑向框架。第三，要求输出「先给失败测试，再最小实现」，能显著减少「先写一大坨 loop 再补测」的回潮。

## 凭据与分发迫使想清的问题

凭据要求逼我面对：keyring 在容器里常常不可用，必须有加密文件回退；`.env` 可以图省事，但必须在 README 写清明文与进程环境风险；status 接口绝不能回显。分发要求逼我面对：没有 Docker 的开发机如何验收？答案是 Python 路径与 CI 镜像构建分工，而不是假装本机有 Docker。这些原本在 demo 里最容易被忽略，却是「别人如何安全跑起来」的真正工程问题。

## 若重做会改变什么

1. 更早引入第二种智能体做冷启动，而不是用自审代替——课程强调的隐性上下文偏差是真实的。  
2. 为 HITL 做完整状态机持久化（当前内存字典，重启即失），并单独开 worktree。  
3. 反馈分类器接入更结构化的 pytest JSON report，而不是正则短输出。  
4. 严格多 PR 历史，即使是个人项目，也按模块合并，方便助教抽查过程。

## 对 Superpowers 的批判

Superpowers 假设：(1) 规格可以在实现前被充分想清；(2) 小步 task + 新鲜子代理能抑制上下文污染；(3) TDD 与两阶段评审能阻止 AI「宣称完成」。在我的项目里，(2)(3) 大体成立；(1) 只部分成立——有些接口真相来自第一次写失败测试时的摩擦。它还隐含「人类有时间逐段签字」；当用户说「完成整个项目」时，签字回路被压缩，方法论的质量保证会泄漏到个人判断力上。结论是：Superpowers 是优秀的防塌方脚手架，但不是判断力的替代品；harness 课最硬核的一问仍然是——**拿掉 LLM 以后，你的仓库还剩多少可验证的工程？**


